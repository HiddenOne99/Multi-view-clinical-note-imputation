name: med
channels:
  - defaults
  - pytorch
  - nvidia
dependencies:
  - python=3.10
  - pip:
      - torch==2.10.0+cu126
      - torchvision==0.25.0+cu126
      - transformers==5.0.0
      - tokenizers==0.22.2
      - huggingface-hub==1.3.7
      - safetensors==0.7.0
      - scikit-learn==1.7.2
      - numpy==2.2.6
      - scipy==1.15.3